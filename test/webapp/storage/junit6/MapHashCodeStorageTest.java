package webapp.storage.junit6;

import webapp.storage.MapHashCodeStorage;

public class MapHashCodeStorageTest extends AbstractStorageTest {
    public MapHashCodeStorageTest() {
        super(new MapHashCodeStorage());
    }
}